---
description: Flutter 大功能开发推进规则
globs: "lib/**/*.dart"
alwaysApply: false
---

# 04-大功能规划

## 1. 模块文档优先

大型功能必须按项目模块开发文档、PRD 或任务 ID 推进，不临场重拆。编码前先确认当前模块文档存在且可读。

## 2. 单模块计划

编码前用当前模块文档形成最小计划：

1. 本轮覆盖哪些任务 ID。
2. 本轮改哪些目录。
3. 本轮不做哪些后续模块能力。
4. Figma 是否已连通（UI 任务）。
5. 验证命令是什么。

## 3. 连续推进规则

编码阶段不在每个模块后自动停下等确认。除非：

- 用户明确要求暂停。
- Figma 无法连接且当前任务是 UI。
- 后端 / 供应商信息缺失导致无法合理 mock。
- `flutter analyze` 或测试无法修复。

## 4. 防扩散

- 不因实现 A 模块顺手实现 B 模块的后续能力。
- 不因当前页面顺手接真实上传、支付或未确认 SDK。
- 不因局部需求顺手决定全局状态机或后端协议。
- 不重写已完成模块的无关代码。
