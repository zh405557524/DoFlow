---
description: Flutter 规则总览、任务分派与编码工作流
globs: "lib/**/*.dart,pubspec.yaml,test/**/*.dart"
alwaysApply: true
---

# 00-规则总览与工作流

## 1. 规则文件索引

| 文件 | 何时读取 |
|------|----------|
| `01-工程与结构.mdc` | 初始化、目录疑问、加依赖 |
| `02-编码规范.mdc` | 任何 Dart 编码 |
| `03-导航.mdc` | 路由注册、跳转、守卫 |
| `04-大功能规划.mdc` | 跨模块大功能 |
| `task-new_feature.mdc` | 新建页面/模块/弹层/路由 |
| `task-ui_task.mdc` | UI 编码、对稿、组件与资源 |
| `task-bug_fix.mdc` | 崩溃、报错、行为异常 |
| `task-logic_change.mdc` | 业务规则、状态流转调整 |
| `task-refactor.mdc` | 不改行为的结构整理 |
| `task-other.mdc` | 无法归类的代码任务 |

**alwaysApply：** `00`、`01`、`02` 始终生效；其余按任务类型读取。

## 2. 权威层级

冲突时按以下顺序：

1. **工程结构、mock/API 分层、注释硬性要求** → `01` / `02`
2. **导航守卫与传参** → `03`；**大模块节奏** → `04`
3. **新建模块的目录 / Controller / 路由 / part 模板** → `task-new_feature`
4. **UI 组件 / 资源 / 弹框 / 对稿** → `task-ui_task`
5. **Bug / 逻辑 / 重构 / 其他** → 对应 task 文件

不能直接照搬 task 示例中的旧组件名、资源类名或与工程不一致的写法。

## 3. 编码阶段原则

Flutter 编码阶段是连续执行阶段。用户要求开始代码编写后，按模块文档持续推进，不在每个模块后等待确认。开发完成后由用户统一验收。

本工程当前已完成初始化，后续默认进入「功能开发循环」。只有发现项目 `.cursor/rules/` 缺失、基础工程结构缺失或 `pubspec.yaml` / 入口文件无法识别时，才重新进入初始化分支。

## 4. 每轮输入先判断

1. 是否是规则输出 / 架构确认任务。
2. 是否存在初始化缺口：
   - `.cursor/rules/` 项目规则缺失。
   - `pubspec.yaml` 缺失或不可识别。
   - `lib/main.dart`、`lib/global.dart`、`lib/routes/`、`lib/services/`、`lib/store/`、`lib/utils/`、`lib/widgets/`、`lib/pages/` 任一基础结构缺失。
3. 若没有初始化缺口，进入功能开发循环。

规则输出任务完成后需要暂停等待用户确认。

## 5. 任务分派

| 任务类型 | 读取规则 | 使用场景 |
|----------|----------|----------|
| 项目初始化 | `01-工程与结构.mdc` | 工程未 init 或基础结构缺失 |
| 新功能开发 | `task-new_feature.mdc` + 按需 `03-导航.mdc` | 新建页面、模块、弹层、路由、Controller |
| UI 编码 / 对稿 | `task-ui_task.mdc` | 视觉、图标、布局、状态 UI、Figma 对稿 |
| Bug 修复 | `task-bug_fix.mdc` | 崩溃、报错、行为异常、测试失败 |
| 业务逻辑修改 | `task-logic_change.mdc` | 业务规则、状态流转、接口调用顺序调整 |
| 重构 | `task-refactor.mdc` | 不改变行为的结构整理 |
| 大功能 / 跨模块 | `04-大功能规划.mdc` + 对应 task | 跨模块大功能 |
| 其他 | `task-other.mdc` | 无法明确归类的 Flutter 代码任务 |

多轮交互时，按用户最新反馈重新判断任务类型；若用户只说「继续」，默认继续当前功能开发任务。

## 6. 实现顺序

```text
模型 / 参数
  -> 真实 API 静态方法
  -> Store / Service
  -> Controller
  -> Controller 私有 mock / real 分支
  -> View / Widgets / Dialog
  -> 状态补齐
  -> analyze / test
  -> 记录结果
```

## 7. 验证

每轮至少执行：

```bash
flutter analyze
```

如测试存在或改动影响主链路，执行：

```bash
flutter test
```

## 8. 记录

完成编码后，需要记录本轮完成的模块、验证命令和遗留风险。流程侧记录由流程工作区负责，不写入本工程规则。

## 9. 暂停条件

只在以下情况暂停：

- 用户明确要求暂停或确认。
- 规则输出完成，需要用户检查规则。
- Figma 未连接且当前任务需要 UI。
- 验证失败且无法自行修复。
- 需求、接口或供应商缺失导致 mock 也无法合理表达。
